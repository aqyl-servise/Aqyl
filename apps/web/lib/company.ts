/**
 * Реквизиты юридического лица для подвала сайта и документов.
 *
 * Без реквизитов платёжный оператор (Kaspi) не подключит мерчанта, а магазины
 * приложений отклонят публикацию. Заполнить перед подачей на публикацию.
 *
 * Поля, оставленные пустыми, в подвале не отображаются — чтобы на сайт
 * никогда не попали выдуманные БИН или адрес.
 */
export const COMPANY = {
  /** Полное наименование юридического лица, напр. «ТОО «SarbonLab»» */
  legalName: "",
  /** БИН — 12 цифр */
  bin: "",
  /** Юридический адрес */
  address: "",
  /** Контактный телефон */
  phone: "",
  /** Адрес электронной почты для обращений */
  email: "support@aqyl-service.kz",
} as const;

/** Заполнены ли реквизиты настолько, чтобы показывать блок в подвале. */
export function hasRequisites(): boolean {
  return Boolean(COMPANY.legalName && COMPANY.bin);
}
